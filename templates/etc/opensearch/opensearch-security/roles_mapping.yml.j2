---
{{ ansible_managed | comment }}

_meta:
  type: "rolesmapping"
  config_version: 2

{% if opensearch_roles_mappings | length > 0 %}
{{ opensearch_roles_mappings | to_nice_yaml(indent=2) }}
{% endif %}
